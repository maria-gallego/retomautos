<div class="bg-light">
  <div class="container">
    <div class="pt-5">
      <h2 class="text-center font-weight-bold text-dark"> Nuestros Carros</h2>
      <p class="pl-4 pt-3 text-muted lead">Encuentra aquí los mejores vehículos usados multimarca del mercado. Nos especializamos en vehículos gama alta y gama media.</p>
    </div>
  </div>

  <div class="container">
    <div id="carCarousel" class="carousel slide text-muted" data-ride="carousel" data-interval="7000">
      <div class="carousel-inner">
        <!--Slide 1 -->
        <div class="carousel-item active">
          <div class="container mb-4 mb-md-5">
            <div class="row pb-4 pt-4">
              <% @slide1_cars.each do |car| %>
                <div class="col-sm mb-3 mb-md-0">
                  <%= render 'visitors/car_card', car: car %>
                </div>
              <% end %>
            </div>
            <!-- Boostrap's display utility classes didn't work to hide row because they conflict with the carousel -->
            <% unless browser.device.mobile? %>
              <div class="row pb-4 pt-4">
                <% @slide2_cars.each do |car| %>
                  <div class="col-sm mb-3 mb-md-0">
                    <%= render 'visitors/car_card', car: car %>
                  </div>
                <% end %>
              </div>
            <% end %>
          </div>
        </div>
        <!--End Slide 1 -->

        <!--Slide 2-->
        <div class="carousel-item">
          <div class="container mb-4 mb-md-5">
            <div class="row pb-4 pt-4">
              <% @slide3_cars.each do |car| %>
                <div class="col-sm mb-3 mb-md-0">
                  <%= render 'visitors/car_card', car: car %>
                </div>
              <% end %>
            </div>
            <!-- Boostrap's display utility classes didn't work to hide row because they conflict with the carousel -->
            <% unless browser.device.mobile? %>
              <div class="row pb-4 pt-4">
                <% @slide4_cars.each do |car| %>
                  <div class="col-sm mb-3 mb-md-0">
                    <%= render 'visitors/car_card', car: car %>
                  </div>
                <% end %>
              </div>
            <% end %>
          </div>
        </div>
        <!--End Slide 2-->
      </div>

      <ol class="carousel-indicators">
        <li style="background-color: #2E2F30" data-target="#carCarousel" data-slide-to="0" class="active"></li>
        <li style="background-color: #2E2F30" data-target="#carCarousel" data-slide-to="1"></li>
      </ol>

    </div>
  </div>


  <div class="text-center pb-5">
    <a class="btn btn-primary btn-lg" href="https://vehiculos.tucarro.com.co/_CustId_28140600" role="button"
       target="_blank">Ver el listado completo</a>
    <div class="pt-4">
      <h5> ¿No encontraste lo que buscabas?
      <%= link_to '¡Nosotros te ayudamos!', new_find_car_request_path %>
      </h5>
    </div>
  </div>

</div>